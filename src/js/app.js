import'./imports';
import {Custom} from './custom'


(function(window, document, $, undefined) {
	"use strict";
	  
	var SampleProject = window.SampleProject || (window.SampleProject = {});	
	SampleProject.dev = true; // This bool can be used to ensure that .

})(window, document, window.jQuery);

/**
 * 
 */
$(function() {
	
	let customObject = new Custom();
});